# PostgreSQL (Neon in production; any Postgres 14+ works for local dev)
DATABASE_URL="postgresql://user:password@localhost:5432/jetta_sport"
DIRECT_URL="postgresql://user:password@localhost:5432/jetta_sport"

# Auth.js (admin panel)
AUTH_SECRET="generate-with-npx-auth-secret"
AUTH_URL="http://localhost:3000"

# Supabase Storage (product/banner/brand images)
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
NEXT_PUBLIC_SUPABASE_URL=""
SUPABASE_STORAGE_BUCKET="jetta-sport-media"

NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Seed-only fallback; the real source of truth is the StoreSettings row,
# editable from /admin/configuracoes.
WHATSAPP_DEFAULT_NUMBER="5511999999999"

# Optional (Phase 5)
NEXT_PUBLIC_GA_ID=""
NEXT_PUBLIC_META_PIXEL_ID=""
